# Memory flush — 2026-06-08 (session continuation #3)

## Session 9 — Full MVP build (afternoon/evening)

### What was built
- **81 source files** in `sensident/` monorepo (Next.js 14 + TypeScript + Drizzle)
- **29 API routes**, **25 React pages**
- **Server running** on `http://localhost:3001` (Node 24.15, libsql SQLite, no Docker)
- **DB initialized** at `dev.db` with 14 tables + 5 newsletter templates
- **1 article seeded** (brossage BASS, 5 slides + long format)
- **First admin created**:
  - email: `paul@sensident.fr`
  - password: `HjmM22pnw_LvlybMJBAhaocl`
  - TOTP secret: `JVOTEWB4GZMXGNKL`
  - role: superadmin

### Stack decisions enforced
- **SQLite via libsql** in dev (zero install, better-sqlite3 failed to compile natively on Windows)
- **PostgreSQL HDS** in prod (full RLS schema in `src/db/schema.sql`)
- **No NestJS separation** — Next.js 14 App Router handles both front and back
- **No Redis** — jobs via pg_cron / Vercel cron
- **No-AI guard script** (`scripts/check-no-ai.js`) blocks any LLM dep

### Modules completed
- Practitioner auth (signup + login + TOTP MFA mandatory)
- Patient flow (R1 landing → double opt-in → magic link → espace → désabonnement)
- Article reader (5-slides F4 + format long, JS heartbeat tracking T1)
- Admin module: CRUD articles, list cabinets, audit logs, settings
- 7 praticien tabs: Vue d'ensemble, Newsletter (4-step composer), Invitations (QR codes), Analytics (M2 entonnoir + heatmap), Engagement (rétention M0/M+1/M+2, segmentation déterministe), Contact (B2 flexible), Mon compte (MFA reset, password, abonnement)
- 5 newsletter templates (moderne, chaleureux, classique, épure, premium) — anti-compérage wording
- Stripe L2 (3 plans, webhooks, coupon AMBASSADOR_2026 100% off 6 mois)
- PWA manifest + service worker
- 6 E2E tests Playwright

### Bug fixed
- **Admin login infinite redirect loop**: moved `/admin/login/*` → `/admin-auth/login/*` (route group to avoid inheriting admin layout)
- Files renamed, all `router.push('/admin/login')` updated to `/admin-auth/login`

### Key files
- `CADRAGE-MVP.md` (20 Ko, 12 sections) — single source of truth
- `MEMORY.md` — 23 decisions, rename Tartrinator → Sensident
- `PROMPTS-IMAGES.md` — 10 prompts for banana/Google image gen (logo priority)
- `SECURITY-CHECKLIST.md` — 60% prod-ready, lists bloquants (HDS, juridique, pentest, backups, rate limit, CSP)
- `GUIDE-DEMARRAGE.md` — Paul onboarding doc

### Pending (Paul's side)
- Generate images with banana/Google from `PROMPTS-IMAGES.md` (logo + favicon priority)
- Write 9 more seed articles via `/admin/articles/new`
- Validate juridique (CGU, contrats, Politique de confidentialité)

### Pending (my side, autonomous)
- Rate limiting middleware (login, opt-in, magic link)
- CSP + HSTS headers in production
- PGP encryption of patient emails at rest
- Endpoint "forget me" + export RGPD
- Retention cleanup cron (3 ans post-unsubscribe)
- 9 articles seed (no — that's Paul)
- Pentest prep (security headers audit)
- Backup encryption strategy doc

### Operating mode established
- **No batch questions** — une par une au fil de l'eau
- **Autonomous** when no blocking decision — Paul pinged only for real choices
- **Compromis technique accepté** : SQLite local (RLS sera testée en staging HDS), pas de Docker
- **Coût infra MVP** : 0 €/mois en dev, ~30-100 €/mois cible HDS

### Next step
- Generate images (Paul with banana)
- Ping Paul when prêt pour le logo + intégration dans le code
- Continue auto-improvement: rate limit, PGP, CSP headers
